// ---------- Rendering ----------
function camCoords(x,z){const dx=x-state.player.x,dz=z-state.player.z,ca=Math.cos(state.cam),sa=Math.sin(state.cam);return{right:ca*dx-sa*dz,forward:-sa*dx-ca*dz}}
function project(x,z,h=0){const c=camCoords(x,z),p=clamp(1/(1+c.forward/210),.47,2.1);return{x:W/2+c.right*5.55*p,y:H*.79-c.forward*3.8*p-h*1.42*p,s:p,d:c.forward}}
function circle(x,y,r,fill,stroke=null,w=1,a=1){ctx.save();ctx.globalAlpha=a;ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);if(fill){ctx.fillStyle=fill;ctx.fill()}if(stroke){ctx.strokeStyle=stroke;ctx.lineWidth=w;ctx.stroke()}ctx.restore()}
function ellipse(x,y,rx,ry,fill,a=1){ctx.save();ctx.globalAlpha=a;ctx.beginPath();ctx.ellipse(x,y,rx,ry,0,0,Math.PI*2);ctx.fillStyle=fill;ctx.fill();ctx.restore()}
function text(t,x,y,size,col='#fff',weight='800'){ctx.save();ctx.fillStyle=col;ctx.font=`${weight} ${size}px system-ui`;ctx.textAlign='center';ctx.textBaseline='middle';ctx.shadowColor='rgba(0,0,0,.8)';ctx.shadowBlur=4;ctx.fillText(t,x,y);ctx.restore()}
function line(points,col,w=1,a=1){ctx.save();ctx.globalAlpha=a;ctx.strokeStyle=col;ctx.lineWidth=w;ctx.lineJoin='round';ctx.lineCap='round';ctx.beginPath();ctx.moveTo(points[0].x,points[0].y);for(let i=1;i<points.length;i++)ctx.lineTo(points[i].x,points[i].y);ctx.stroke();ctx.restore()}
function poly(points,fill,stroke=null,w=1,a=1){ctx.save();ctx.globalAlpha=a;ctx.beginPath();ctx.moveTo(points[0].x,points[0].y);for(let i=1;i<points.length;i++)ctx.lineTo(points[i].x,points[i].y);ctx.closePath();if(fill){ctx.fillStyle=fill;ctx.fill()}if(stroke){ctx.strokeStyle=stroke;ctx.lineWidth=w;ctx.stroke()}ctx.restore()}
function groundCircle(x,z,r,fill,stroke,w=1,a=1){const p=[];for(let i=0;i<42;i++){const q=i/42*Math.PI*2;p.push(project(x+Math.cos(q)*r,z+Math.sin(q)*r))}poly(p,fill,stroke,w,a)}
function drawFloor(){const g=ctx.createLinearGradient(0,0,0,H);g.addColorStop(0,'#777d95');g.addColorStop(.28,'#4b526b');g.addColorStop(.52,'#252b40');g.addColorStop(.75,'#121725');g.addColorStop(1,'#070910');ctx.fillStyle=g;ctx.fillRect(0,0,W,H);for(let i=0;i<48;i++){const a0=i/48*Math.PI*2,a1=(i+1)/48*Math.PI*2,p=[project(0,0)];for(let j=0;j<=4;j++){const a=a0+(a1-a0)*j/4;p.push(project(Math.cos(a)*ARENA,Math.sin(a)*ARENA))}poly(p,i%2?'#151c30':'#202941')}for(const rr of[16,30,44,58,72,86,98]){const p=[];for(let i=0;i<=100;i++){const a=i/100*Math.PI*2;p.push(project(Math.cos(a)*rr,Math.sin(a)*rr))}line(p,rr===98?'#7aa9c2':'#33445f',rr===98?4:1.1,rr===98?.95:.7)}for(let i=0;i<32;i++){const a=i*Math.PI/16;line([project(Math.cos(a)*8,Math.sin(a)*8),project(Math.cos(a)*98,Math.sin(a)*98)],i%4?'#26344e':'#526985',i%4?.8:1.7,.55)}}
function drawWay(label,deg){const p=pt(deg,86),g=project(p.x,p.z),s=g.s;ctx.save();ctx.translate(g.x,g.y);ctx.scale(s,s);ctx.fillStyle=/[A-D]/.test(label)?'rgba(194,70,86,.93)':'rgba(72,118,201,.93)';ctx.strokeStyle='rgba(255,255,255,.82)';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(0,-21);ctx.lineTo(15,-8);ctx.lineTo(15,18);ctx.quadraticCurveTo(15,25,7,25);ctx.lineTo(-7,25);ctx.quadraticCurveTo(-15,25,-15,18);ctx.lineTo(-15,-8);ctx.closePath();ctx.fill();ctx.stroke();text(label,0,2,17);ctx.restore()}
function drawBlackHole(h,t){const g=project(h.x,h.z,2.5),s=g.s,pulse=1+.08*Math.sin(t*3+h.x);ellipse(g.x,g.y+7*s,14*s,5*s,'rgba(0,0,0,.45)');ctx.save();ctx.shadowColor='#8c68ff';ctx.shadowBlur=18*s;circle(g.x,g.y,12.5*s*pulse,'#030407','#8d73ff',2.4*s,.98);ctx.restore();circle(g.x,g.y,8.2*s,'#000','#402b6c',2*s);for(let i=0;i<3;i++){ctx.save();ctx.translate(g.x,g.y);ctx.rotate(t*.6+i*2.1);ctx.strokeStyle='rgba(157,223,255,.55)';ctx.lineWidth=1.5*s;ctx.beginPath();ctx.ellipse(0,0,15*s,6*s,0,.4,2.8);ctx.stroke();ctx.restore()}}
function drawBoss(pos,name,type){const g=project(pos.x,pos.z),s=g.s;ellipse(g.x,g.y+10*s,26*s,8*s,'rgba(0,0,0,.4)');ctx.save();ctx.translate(g.x,g.y-25*s);ctx.scale(s,s);if(type==='chaos'){ctx.fillStyle='#6f4631';ctx.strokeStyle='#f0ae6b';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(0,-42);ctx.lineTo(-28,8);ctx.lineTo(-22,44);ctx.lineTo(0,28);ctx.lineTo(24,44);ctx.lineTo(30,8);ctx.closePath();ctx.fill();ctx.stroke();circle(0,-50,13,'#80604c','#ffd0a0',2)}else{ctx.fillStyle='#554686';ctx.strokeStyle='#b9aaff';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(0,-56);ctx.lineTo(-23,-18);ctx.lineTo(-31,43);ctx.lineTo(0,58);ctx.lineTo(31,43);ctx.lineTo(23,-18);ctx.closePath();ctx.fill();ctx.stroke();circle(0,-65,12,'#d7c7b8','#e5dcff',2);ctx.strokeStyle='#8d79d8';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(-16,-39);ctx.lineTo(-34,-4);ctx.moveTo(16,-39);ctx.lineTo(34,-4);ctx.stroke()}ctx.restore();text(name,g.x,g.y-100*s,14*s,type==='chaos'?'#ffd0a0':'#ddd5ff')}
function giantPos(k){return pt(k,112)}
function drawKefka(t){const k=kAngleAt(t),p=giantPos(k),g=project(p.x,p.z),s=g.s*1.05;ellipse(g.x,g.y+15*s,38*s,11*s,'rgba(0,0,0,.4)');ctx.save();ctx.translate(g.x,g.y-7*s);ctx.scale(s,s);ctx.fillStyle='#6b3479';ctx.strokeStyle='#f9b4ff';ctx.lineWidth=1.8;ctx.beginPath();ctx.moveTo(0,-70);ctx.quadraticCurveTo(-48,-46,-56,12);ctx.lineTo(-42,68);ctx.lineTo(-7,76);ctx.lineTo(0,91);ctx.lineTo(7,76);ctx.lineTo(42,68);ctx.lineTo(56,12);ctx.quadraticCurveTo(48,-46,0,-70);ctx.closePath();ctx.fill();ctx.stroke();circle(0,-82,13,'#efc2ae','#fff',1.3);ctx.fillStyle='#f3d7b2';ctx.beginPath();ctx.moveTo(-11,-93);ctx.quadraticCurveTo(-48,-116,-58,-90);ctx.quadraticCurveTo(-28,-104,-20,-79);ctx.quadraticCurveTo(-7,-113,0,-103);ctx.quadraticCurveTo(13,-118,21,-79);ctx.quadraticCurveTo(34,-104,58,-90);ctx.quadraticCurveTo(29,-104,18,-77);ctx.closePath();ctx.fill();ctx.restore();text('GIANT KEFKA',g.x,g.y-155*s,13*s,'#f4dcff')}
function drawBodySlam(t){const active=BODY.find(b=>t>=b.tele-.35&&t<=b.hit+.75);if(!active)return;const k=state.kAngles[2],axis=pt(k,1),perp={x:-axis.z,z:axis.x},hw=34,L=105;const p=[project(axis.x*L+perp.x*hw,axis.z*L+perp.z*hw),project(-axis.x*L+perp.x*hw,-axis.z*L+perp.z*hw),project(-axis.x*L-perp.x*hw,-axis.z*L-perp.z*hw),project(axis.x*L-perp.x*hw,axis.z*L-perp.z*hw)];poly(p,'rgba(244,92,53,.25)','#ff8a62',2.4,.92);const kp=giantPos(k),g=project(kp.x*.32,kp.z*.32,3);ctx.save();ctx.translate(g.x,g.y);ctx.rotate(-rad(k-state.cam*180/Math.PI));ctx.fillStyle='rgba(132,53,141,.95)';ctx.fillRect(-90*g.s,-15*g.s,180*g.s,30*g.s);ctx.restore();text('LOOK UPON ME AND DESPAIR',W/2,126,14,'#ffbc9b')}
function drawGroundSector(center,face,half,r,fill,a=.25){const pts=[project(center.x,center.z)];for(let d=-half;d<=half;d+=5){const p=pt(face+d,r);pts.push(project(center.x+p.x,center.z+p.z))}poly(pts,fill,null,0,a)}
function drawDamning(t){for(const d of DAMN){if(t>=d.start&&t<=d.hit+.3){drawGroundSector(CHAOS_POS,state.damnFacing[d.id-1],90,150,'#df542a',.22);text('DAMNING EDICT',W/2,108,14,'#ffb090')}}}
function drawLat(t){if(t<LAT.start||t>LAT.hit2+.5)return;const f=norm(state.kAngles[2]+45),first=t<LAT.hit1+.15;let centers;if((first&&state.latSidesFirst)||(!first&&!state.latSidesFirst))centers=[f+90,f-90];else centers=[f,f+180];centers.forEach(a=>drawGroundSector(CHAOS_POS,norm(a),63.75,150,'#7e5bd4',.18));text(state.latSidesFirst?'LATITUDINAL IMPLOSION':'LONGITUDINAL IMPLOSION',W/2,108,13,'#cbb8ff')}
function drawSlap(t){for(const s of SLAPS){if(t<s.start||t>s.center+.4)continue;for(let i=0;i<3;i++){const c=slapCircle(s,i),hit=s.hits[i];if(t>=hit-1.25&&t<=hit+.25){groundCircle(c.x,c.z,50,'rgba(236,148,86,.13)','#f6aa72',2,.9)}}if(t>=s.hits[2]-.1&&t<=s.center+.25)groundCircle(0,0,26,'rgba(219,156,255,.15)','#dca4ff',2,.9);const hand=state.slapLeft[s.id-1]?'LEFT HAND → ROLE CONES':'RIGHT HAND → PARTY STACK';text(hand,W/2,90,13,'#ffd2f2')}}
function drawCastBar(pos,name,start,end,t,color){if(t<start||t>end)return;const g=project(pos.x,pos.z,20),w=150*g.s,p=clamp((t-start)/(end-start),0,1);ctx.fillStyle='rgba(9,12,20,.78)';ctx.fillRect(g.x-w/2,g.y-5,w,12);ctx.fillStyle=color;ctx.fillRect(g.x-w/2,g.y-5,w*p,12);text(name,g.x,g.y-16,11*g.s,'#fff')}
